// local
import {theme} from 'theme';

export const config = {
  STOP: {top: theme.red, middle: theme.grey, bottom: theme.grey},
  YIELD: {top: theme.grey, middle: theme.yellow, bottom: theme.grey},
  GO: {top: theme.grey, middle: theme.grey, bottom: theme.green},
  OFF: {top: theme.grey, middle: theme.grey, bottom: theme.grey},
};
